# Development happens on Windows, the application runs in a Linux container.
# Keeping LF everywhere avoids phantom diffs and broken shell scripts in the image.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.pdf binary
